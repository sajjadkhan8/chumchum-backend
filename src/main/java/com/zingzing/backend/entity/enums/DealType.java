package com.zingzing.backend.entity.enums;

/**
 * Canonical deal type enum used across packages, orders, and offers.
 * Replaces the legacy PackagePricingType enum.
 */
public enum DealType {
    PAID,
    BARTER,
    HYBRID
}
