alter table brands
    add column preferred_creator_categories varchar(500),
    add column target_cities varchar(500),
    add column target_platforms varchar(500),
    add column campaign_budget_range varchar(150);
